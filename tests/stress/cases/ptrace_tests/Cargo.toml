[package]
name = "ptrace_tests"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
nix = { version = "0.28", default-features = false, features = ["ptrace", "process", "signal"] }

[profile.release]
panic = "abort"
