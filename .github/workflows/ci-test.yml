name: ci-test

on:
  push:
    branches: [ '**' ]
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-${{ github.event_name }}

jobs:
  ci:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        arch: [aarch64, x86_64]
    env:
      ARCH: ${{ matrix.arch }}
      XKERNEL_ROOT: ${{ github.workspace }}/.cache/X-Kernel
    steps:
      - name: Checkout starry-test-harness
        uses: actions/checkout@v4

      - name: Create cache directory
        run: mkdir -p .cache

      - name: Checkout X-Kernel
        uses: actions/checkout@v4
        with:
          repository: kylin-x-kernel/x-kernel
          path: .cache/X-Kernel
          submodules: recursive
          ssh-key: ${{ secrets.XKERNEL_DEPLOY_KEY }}

      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: starry-ci-harness-${{ matrix.arch }}

      - name: Install musl toolchain
        uses: arceos-org/setup-musl@v1
        with:
          arch: ${{ matrix.arch }}

      - name: Debug musl toolchain paths
        run: |
          echo "=== Environment variables ==="
          env | grep -i musl || true
          echo "=== Which gcc ==="
          which ${{ matrix.arch }}-linux-musl-gcc || true
          echo "=== GCC search dirs ==="
          ${{ matrix.arch }}-linux-musl-gcc -print-search-dirs || true
          echo "=== Find include dirs ==="
          dirname $(which ${{ matrix.arch }}-linux-musl-gcc) | xargs -I{} find {}/../ -type d -name include 2>/dev/null | head -20

      - name: Install QEMU
        uses: arceos-org/setup-qemu@v1
        with:
          version: 10.1.0
          arch_list: ${{ matrix.arch }}

      - name: Install filesystem tools
        run: sudo apt-get update && sudo apt-get install -y e2fsprogs

      - name: Install Rust kernel target
        run: |
          case "${ARCH}" in
            aarch64) KERNEL_TARGET="aarch64-unknown-none-softfloat" ;;
            x86_64) KERNEL_TARGET="x86_64-unknown-none" ;;
            *) KERNEL_TARGET="${ARCH}-unknown-none" ;;
          esac
          TOOLCHAIN="$(cd .cache/X-Kernel && rustup show active-toolchain 2>/dev/null | awk '{print $1}')"
          if [[ -n "${TOOLCHAIN}" ]]; then
            rustup target add "${KERNEL_TARGET}" --toolchain "${TOOLCHAIN}"
          else
            rustup target add "${KERNEL_TARGET}"
          fi

      - name: 执行 ci-test
        run: |
          MUSL_GCC=$(which ${{ matrix.arch }}-linux-musl-gcc)
          MUSL_ROOT=$(dirname "$MUSL_GCC")/..
          MUSL_INCLUDE="$MUSL_ROOT/${{ matrix.arch }}-linux-musl/include"
            
          echo "MUSL_ROOT=$MUSL_ROOT"
          echo "MUSL_INCLUDE=$MUSL_INCLUDE"
            
          export CC="$MUSL_GCC"
          export CFLAGS="-I$MUSL_INCLUDE"
          export CARGO_TARGET_AARCH64_UNKNOWN_LINUX_MUSL_LINKER="$MUSL_GCC"
          export CARGO_TARGET_X86_64_UNKNOWN_LINUX_MUSL_LINKER="$MUSL_GCC"
          export BINDGEN_EXTRA_CLANG_ARGS="--sysroot=/dev/null"
          make ci-test run

      # - name: Dump suite logs
      #   if: always()
      #   run: |
      #     if [ -d logs/ci ]; then
      #       find logs/ci -name 'suite.log' -print -exec sh -c 'echo "::group::{}"; cat "$1"; echo "::endgroup::"' _ {} \;
      #     else
      #       echo "logs/ci not found"
      #     fi

      # - name: Upload suite artifacts
      #   if: always()
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: ci-logs-${{ github.run_id }}
      #     path: logs/ci
      #     if-no-files-found: warn
